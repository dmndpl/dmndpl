# Cheatsheet and Random stuff

##WezTerm
alt + enter - toggle fullscreen

## TMUX
tmux
tmux attach
tmux ls
alt + b - wake tmux up
arrow keys to navigate panels
" vertical split
% horizontal split
d to delete

## UV - faster Python package manager
curl -LsSf https://astral.sh/uv/install.sh | sh
uv init
uv venv
source .venv/bin/activate
uv add {DEPENDENCY_NAME}
